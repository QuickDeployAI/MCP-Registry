---
"@quickdeployai/arazzo-2-mcp": minor
---

Add a runtime-expression evaluator (`$inputs`, `$steps.<id>.outputs/response`, `$sourceDescriptions`, `$response.body`, `$statusCode`, with RFC 6901 JSON Pointer support) and a `successCriteria` evaluator supporting `simple`, `regex`, and `jsonpath` criteria kinds.
