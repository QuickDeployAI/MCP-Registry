/**
 * Schema constants for the query layer.
 */

/**
 * Character threshold above which a string field value is considered
 * "large" and replaced by a content-ref marker in default results.
 * Use get_feed_item to retrieve large fields individually.
 */
export const LARGE_FIELD_THRESHOLD = 500;

